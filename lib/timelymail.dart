import 'package:flutter/material.dart';
import 'package:timelymail/screens/auth.dart';
import 'package:timelymail/screens/dashboard.dart';

class Timelymail extends StatelessWidget {
  const Timelymail({super.key});

  @override
  Widget build(BuildContext context) {
    //return Auth();
    return Dashboard();
  }
}
